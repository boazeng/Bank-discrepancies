# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Environment (credentials — never commit)
.env

# Node
node_modules/

# Logs
*.log
server_*.txt
server_*.log

# Claude local settings
.claude/

# Runtime state — data lives in the volume, not git
database/receipts/*.json

# Recommendations SQLite DB (runtime state, lives in the volume)
database/receipts/*.db
database/receipts/*.db-*

# Built frontend — built inside the Docker image (multi-stage), not committed
dist/
